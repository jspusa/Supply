# Keep the public workspace local to the browser

The public GitHub Pages workspace will restore raw source files and H10 text from versioned browser storage, with small preferences kept separately, and will not send those inputs to a new cloud service. This preserves the existing client-only privacy boundary and offline-friendly workflow at the cost of no cross-device sync; the Boss entrypoint may continue using its existing authenticated cloud adapter.
